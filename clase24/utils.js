// primera forma

// export const sum = (a, b) => a + b

// export const substract = (a, b) => a - b

// export const multiply = (a, b) => a * b

// segunda forma exportar

const sum = (a, b) => a + b;

const substract = (a, b) => a - b;

const multiply = (a, b) => a * b;

export default {
  sum,
  substract,
  multiply
};
